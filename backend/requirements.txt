# --- Web framework ----------------------------------------------------------
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
pydantic[email]>=2.6.0
python-multipart>=0.0.9

# --- Security ---------------------------------------------------------------
bcrypt>=4.1.0
python-jose[cryptography]>=3.3.0

# --- Persistence ------------------------------------------------------------
# Optional at runtime: without MONGODB_URI the app uses the bundled JSON store.
pymongo>=4.6.0
dnspython>=2.5.0
python-dotenv>=1.0.0

# --- Image processing & OCR -------------------------------------------------
pillow>=10.2.0
numpy>=1.26.0
opencv-python-headless>=4.9.0.80
rapidocr-onnxruntime>=1.3.0

# --- Report generation ------------------------------------------------------
reportlab>=4.1.0        # PDF with real, selectable text
python-docx>=1.1.0      # editable DOCX notices
openpyxl>=3.1.0         # bulk XLSX export

# --- Utilities --------------------------------------------------------------
python-dateutil>=2.8.2
requests>=2.31.0

# --- Tests ------------------------------------------------------------------
pytest>=8.0.0
httpx>=0.27.0
